# 行知 Navi · 产品介绍

## ❗ 必填

| **所在小组** | 第五组-AI大王 | **作品主题** | 行知 Navi | **DEMO链接** | https://xingzhi-navi.popo.baidu-int.com |
|-|-|-|-|-|-|

| **使用的厂内外工具** | duok（原型搭建） + comate（产品上传） + ducc（产品优化） |
|-|-|

> 说明：工具这一栏我无法从现有材料推断。可确认的事实是——页面本体是零依赖的纯静态实现（原生 JS + 手写 SVG 布局，无构建步骤、无第三方库，全部文件约 50 KB），文案改写由 Comate 完成。页面本体是否也由 AI 工具生成、用的是哪个，需要你补充。

### 作品的痛点（场景）

下午六点，同事在群里问「那个方案明天能出吗」，你愣了一下。手里同时摊着需求文档、访谈记录、待办清单和三篇没读完的笔记，每一处都记着一点进展，但没有哪个地方能让你一眼说出「现在到底什么状态、卡在谁那儿」。翻了两分钟聊天记录你才想起来：方案卡在还差两场访谈上。

这不是事太多的问题——事情本身并不多。问题是它们散在四五个地方，而且没有任何一个地方会告诉你：卡点在哪、下一步该做什么。于是每次开始做事之前，都要先花十几分钟重新想起来自己在做什么；一天下来，真正动手的时间被这些「回忆」和「翻找」切成了碎片。真正需要被看见的，不是「还有多少事没做」，而是「哪件事卡住了、卡在哪、我此刻能做的最小一步是什么」。

---

## 📌 创意背景 - 产品定位、目标人群

> （为什么要做这个应用）

**创意来源，为什么有这个创意，解决什么问题：**

针对同时推进多条事务的知识工作者与学生，在「每天开始工作」和「每周复盘」的场景下，需要「把散落各处的状态收拢成一张图、并直接得到下一个可执行动作」的需求。

常见的工具解决的是「记录」：待办清单负责存事项，笔记软件负责存内容，聊天记录负责存过程。它们都没有回答一个问题——**现在卡在哪**。而且记录本身会变成新的负担：清单越长，越不想打开。

行知 Navi 的定位不是任务管理工具，也不是日程表，而是**个人状态的操作系统**。它只回答两个问题：

1. 现在卡在哪？
2. 下一步做什么？

目标人群是手上同时有多条线、且状态分散在多个工具里的人——尤其是「事情不算多，但总是要先花时间回忆」的那类人。

---

## 🎯 解决方案 - 作品如何实现

### 产品设计路径

**1. 用一张图替代一份清单。** 把个人事务固定收进四个领域——工作、学习、生活、项目——每个领域下挂项目，项目下挂具体的事，形成一棵树。打开页面看到的是结构，不是流水账。

**2. 状态从叶子自下而上长出来。** 每件具体的事带一个状态（顺利 / 正在做 / 卡住了 / 在等前面的事 / 已完成）。上层节点的状态不需要手工维护，由子节点按优先级聚合推导：只要下面有卡住的，父节点就是卡住的。进度同理，取子节点均值。这意味着**用户只需要维护最细的那一层**，上面会自己算出来。

**3. 把「卡点」变成一等公民。** 每个节点都可以记录「卡在哪里」和「在等什么」。顶部有「只看卡住的」筛选，一键把图上其他内容收起来，只留下真正堵住的地方。

**4. 每张卡片都给出下一步，而不是一句加油。** 点开任意节点，右侧给的不是「继续努力」，而是三条具体信息：下一步做什么（一句话动作）、大约要多久（分钟）、以及一句执行提示（怎么开始最省力）。例如不是「推进访谈」，而是「给两位各发一封邮件，把周四和周五各占一个时段，这次不用准备提纲」。「下一步」不是写死的：没填的时候会按状态、依赖和子节点自动推断，并标上「帮你想的」——卡住的把「卡在哪里」变成第一步，在等前面事的先指到该做完的依赖，领域 / 项目则指向里面最该先动的那件事。首屏还会用同一套规则挑出「现在最该做的一件事」，一进来就知道该动哪儿。

**5. 从「看到」到「做到」只差一次点击。** 点「开始做」进入专注计时，弹出一张只有这件事和倒计时的卡片，其余界面暗下去。做完点「做完了」，这件事标记完成，**依赖它、原本「在等前面的事」的节点会自动变成「正在做」**——一件事完成会顺着依赖链把后面的事推起来。做完的事计入首屏「今天已经做完 N 件」。

**6. 把它当真工具用，而不只是演示。** 每个节点都能新建 / 编辑 / 删除：领域下建项目、项目下建子任务；叶子可直接拖进度条改百分比，拖到 100% 自动完成；编辑弹窗里改名称、下一步、提示、预计分钟、状态，并勾选它「在等哪些事」（依赖）。状态图上还能按住卡片拖动改挂载——项目挪到别的领域下、子任务挪到别的项目下，始终保持「领域 › 项目 › 具体的事」三层结构。所有改动都存在浏览器本地，刷新还在；改状态、拖拽、增删改都能**撤销 / 重做**（`Cmd/Ctrl + Z` / `Shift + Z`）。第一次编辑某位成员时会把内置示例固化成一份可编辑副本，示例场景不会被破坏。

**7. 可选接大模型想「下一步」。** 顶部「AI」里配置一个 OpenAI 兼容接口（OneAPI，默认地址 `https://oneapi-comate.baidu-int.com/v1`），任务 / 项目详情就会多出「让 AI 想一步」：模型读当前状态、卡点与依赖，回一个具体动作并写回该节点（可撤销）。不配也能用——规则推断始终兜底。Key 明文存在本机浏览器、请求只发往你自己配置的接口，仅建议内网 / 本地使用。

**8. 支持多位成员。** 首页列出成员，每位成员各有一张自己的状态图，切换到谁就看谁的。

### 可行性

不依赖任何后端与第三方服务，纯静态页面，浏览器直接打开即可运行，也可以离线使用。数据存在浏览器本地，不需要注册登录。整个作品没有构建步骤，改动即生效——这让迭代文案和交互的成本极低。接大模型是可选项：走标准 OpenAI 兼容接口（OneAPI），不接则由本地规则推断兜底，离线可用这一条始终成立。

---

## 📍 预期效果 - 功能 Demo 演示

**DEMO：** https://xingzhi-navi.popo.baidu-int.com

演示路径建议按「看见状态 → 找到问题 → 获得建议 → 开始行动」走一遍：

1. **进入首页** —— 看到成员列表，每人后面标着「几件卡住了 / 几件还没做完」。
2. **选一位成员进入** —— 展开一张四领域的状态图，顶部实时时钟在走，首屏直接给出「现在最该做的一件事」。
3. **点「只看卡住的」** —— 图上只剩真正被堵住的节点，其余收起。
4. **单击任意节点** —— 右侧出现「现在怎么样 / 卡在哪里 / 在等什么」，以及「下一步」的具体动作、预计耗时和执行提示（没填的会标「帮你想的」）。
5. **点「开始做」** —— 进入专注计时卡片，只剩这件事和倒计时；点「做完了」后，依赖它的、原本在等的节点会自动转为「正在做」。
6. **下钻与依赖** —— 点项目或领域可继续往里看，虚线表示跨节点的依赖关系。
7. **当真工具改** —— 新建 / 编辑 / 删除节点、拖动进度条、拖拽改挂载，随时 `Cmd/Ctrl + Z` 撤销 / 重做；改动存本地，刷新还在。
8. **可选 AI** —— 顶部「AI」填入 OneAPI 接口与 Key 后，详情里「让 AI 想一步」由大模型生成下一步动作。

当前演示数据为虚构示例（4 位成员），用于展示完整的信息结构。想恢复内置示例：浏览器控制台执行 `localStorage.clear()` 后刷新。

---

## 👥 项目亮点描述

**功能亮点**

- **状态自动上浮**：只维护最细一层，父节点状态与进度自动推导，避免「每层都要手动更新」的常见负担。
- **卡点是一等公民**：「卡在哪里」「在等什么」是独立字段，可以一键筛出所有堵住的地方。
- **给动作，不给口号**：每张卡片都带一句可直接执行的动作、一个时间估计和一条执行提示；没填时按状态、依赖、子节点自动推断，首屏还会挑出「现在最该做的一件事」。
- **依赖会自己流动**：一件事做完，依赖它、原本在等的节点自动转为「正在做」，顺着依赖链把后面的事推起来。
- **一行闭环**：从看到问题到开始动手只有一次点击，做完计入「今天已做完 N 件」。
- **是工具不是演示**：节点可增删改、拖进度、拖拽改挂载，全程可撤销 / 重做，改动存本地。
- **AI 可选、规则兜底**：接 OneAPI 让大模型想「下一步」，不接则本地规则推断，离线始终可用。
- **零依赖、可离线**：无构建、无第三方库、无后端，单文件夹拷走就能跑。

**文案亮点**

- 全站避免「闭环」「阻塞」「下钻」这类内部黑话，改用「卡住了」「点开看看里面有什么」，让非技术背景的人也能直接读懂。
- 状态名从「畅通 / 受阻 / 等待依赖 / 进行中」改为「顺利 / 卡住了 / 在等前面的事 / 正在做」，读起来像人话。
- 提示语写成可执行的建议而非评价，例如「别做整本书的笔记，只写三条：脏读、不可重复读、幻读，各举一个自己碰过的例子」。

**设计亮点**

- 状态用颜色而非文字标签呈现，一眼扫过就能看出图的健康度。
- 树形布局与依赖虚线为手写算法（SVG），没有引入任何图表库。

---

## 🗓️ 未来展望

- **从手动录入到自动汇总**：目前需要自己录入事项，下一步可以让状态从已有的工具（日历、文档、聊天）自动收拢，减少维护成本。
- **真正的多人协同**：现在成员数据存在各自浏览器本地，彼此看不到。接入后端后，「谁卡住了」才能跨人可见，这也更贴近团队场景。
- **卡点的历史与规律**：记录每次卡住的原因与解卡耗时，沉淀出「我通常在什么环节卡住」，从看见状态走到看见模式。
- **更细的时间引导**：把「大约要 25 分钟」与实际耗时对照，逐步校准对自己的估计。

可参考[🌟零度AI产品路演指南🌟](https://ku.baidu-int.com/knowledge/HFVrC7hq1Q/94XAO0y0b8/nP5w9TSW6i/KtMDTCevigu6au?t=mention&mt=doc&dt=doc)

**祝各位路演顺利❤️**
